{
  "name": "ai-finance-website",
  "version": "1.0.0",
  "description": "AI-powered personal finance tracker with expense management and analytics",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd client && npm start",
    "install-deps": "cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["finance", "ai", "expense-tracker", "nextjs", "nodejs"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}